# Auto-generated waza eval scaffold for thinking-partner-for-docs.
# Replace executor: mock with copilot-sdk (or another) for real evaluation.
name: thinking-partner-for-docs-eval
description: Initial mock-executor eval suite for the thinking-partner-for-docs skill.
skill: thinking-partner-for-docs
version: "0.1"

config:
  trials_per_task: 1
  timeout_seconds: 300
  parallel: false
  executor: mock
  model: claude-sonnet-4.6

metrics:
  - name: task_completion
    weight: 1.0
    threshold: 0.0

graders:
  - type: code
    name: has_output
    config:
      assertions:
        - "len(output) >= 0"

tasks:
  - "tasks/*.yaml"
