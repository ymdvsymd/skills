id: book-translation-pipeline-vitepress-bilingual-build
name: "vitepress-bilingual-build"
tags:
  - "imported"

inputs:
  prompt: "docs/{en,ja} 並置の VitePress サイトをビルドしたいけど、最終チェック項目は?"

expected:
  outcomes:
    - type: task_completed
  # Imported from legacy evals.json. Assertions below are
  # human-review only; convert to graders (output_contains,
  # regex, etc.) when wiring up real automated evaluation.
  output_contains: []
  behavior:
    max_tool_calls: 100

# Original human-review assertions:
#   - npm run build 成功確認が案内される
#   - 用語一貫性チェック (grep で _glossary.md 用語) が案内される
#   - である調逸脱チェック (です。/ます。 grep) が案内される
#   - 代表章の目視確認が案内される
