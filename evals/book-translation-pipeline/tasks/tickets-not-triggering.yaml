id: book-translation-pipeline-tickets-not-triggering
name: "tickets-not-triggering"
tags:
  - "imported"

inputs:
  prompt: "GitHub の issue を Linear に移行したいんだけど、どうすればいい?"

expected:
  outcomes:
    - type: task_completed
  # Imported from legacy evals.json. Assertions below are
  # human-review only; convert to graders (output_contains,
  # regex, etc.) when wiring up real automated evaluation.
  output_contains: []
  behavior:
    max_tool_calls: 100

# Original human-review assertions:
#   - book-translation-pipeline skill は triggers しない (無関係なため)
