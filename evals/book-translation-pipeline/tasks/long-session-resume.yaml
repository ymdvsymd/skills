id: book-translation-pipeline-long-session-resume
name: "long-session-resume"
tags:
  - "imported"

inputs:
  prompt: "前回のセッションで翻訳作業が rate limit で途中終了した。別マシンの新セッションで続きをやりたい。"

expected:
  outcomes:
    - type: task_completed
  # Imported from legacy evals.json. Assertions below are
  # human-review only; convert to graders (output_contains,
  # regex, etc.) when wiring up real automated evaluation.
  output_contains: []
  behavior:
    max_tool_calls: 100

# Original human-review assertions:
#   - bd dolt pull で同期するよう案内される
#   - bd ready で次のチケットを取得することが案内される
#   - 在庫の in_progress チケットの扱い (claim 解除 or 継続) について言及される
#   - skill が再 triggers されてオーケストレーションループから自動再開することが示される
