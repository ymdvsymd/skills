# Auto-generated waza eval scaffold for worksample-question-generator.
# Replace executor: mock with copilot-sdk (or another) for real evaluation.
name: worksample-question-generator-eval
description: Initial mock-executor eval suite for the worksample-question-generator skill.
skill: worksample-question-generator
version: "0.1"

config:
  trials_per_task: 1
  timeout_seconds: 300
  parallel: false
  executor: mock
  model: claude-sonnet-4-6

graders:
  - type: code
    name: has_output
    config:
      assertions:
        - "len(output) >= 0"

tasks:
  - "tasks/*.yaml"
