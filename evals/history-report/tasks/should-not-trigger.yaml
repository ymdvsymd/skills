id: history-report-should-not-trigger-001
name: "history-report should NOT trigger on unrelated prompt"
tags:
  - "should-not-trigger"
  - "placeholder"

inputs:
  prompt: "<TODO: insert an unrelated prompt that must not invoke this skill>"

expected:
  outcomes:
    - type: task_completed
  behavior:
    max_tool_calls: 100
